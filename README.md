# partial-flag-cracker

Made this script to assist with password cracking CTF challenges. It takes a MD5 hash followed by a partial plain-text password and applies a four digit number at the end. This could also be done with something like hashcat but I sometimes forget the syntax. 
